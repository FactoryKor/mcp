# mcp 자체 의존성
mcp

# 각 진단 도구는 별도 GitHub 리포에서 버전 태그로 고정 설치한다(모노레포 X).
# 태그를 올릴 때는 여기 버전만 바꾸면 되고, 각 컴포넌트 리포의 나머지 의존성은
# 해당 리포 pyproject.toml(dynamic dependencies)을 통해 자동으로 함께 설치된다.
# private 리포라면 로컬은 `gh auth setup-git`으로 인증된 git 자격증명을 그대로 쓰고,
# CI(Docker 빌드)에서는 mcp/Dockerfile의 GH_PAT 시크릿을 통해 인증한다.
pg-diagnose @ git+https://github.com/FactoryKor/pg.git@v1.0.0
aks-diagnose @ git+https://github.com/FactoryKor/AKS-Diag.git@v1.0.0
adx-diagnose @ git+https://github.com/FactoryKor/adx.git@v1.0.0
eh-diagnose @ git+https://github.com/FactoryKor/eh.git@v1.0.0
agw-diagnose @ git+https://github.com/FactoryKor/agw.git@v1.0.0
svcmap-diagnose @ git+https://github.com/FactoryKor/svcmap.git@v1.0.0
